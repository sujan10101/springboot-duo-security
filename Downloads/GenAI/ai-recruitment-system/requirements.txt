# ── Core Agent Framework ──────────────────────────────────────────────────────
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0

# ── LLM Providers (install the one(s) you need) ───────────────────────────────
langchain-ollama>=0.2.0       # LLM_PROVIDER=ollama  (local/cloud)
langchain-openai>=0.2.0       # LLM_PROVIDER=openai  + cloud Ollama + RAG embeddings
langchain-anthropic>=0.3.0    # LLM_PROVIDER=anthropic
langchain-google-genai>=2.0.0 # LLM_PROVIDER=gemini

# ── Raw OpenAI SDK (RAG embeddings via text-embedding-3-small) ────────────────
openai>=1.0.0

# ── Vector Store ──────────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── Google APIs (no longer needed — calendar invite sent via .ics email) ─────
# google-auth>=2.0.0
# google-auth-oauthlib>=1.0.0
# google-api-python-client>=2.0.0

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit>=1.40.0
PyPDF2>=3.0.1

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pytz>=2023.4
typing-extensions>=4.9.0
